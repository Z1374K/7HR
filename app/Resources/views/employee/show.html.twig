{% extends 'base.html.twig' %}

{% block body %}
    <h1>Employee</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ employee.id }}</td>
            </tr>
            <tr>
                <th>Nazwisko</th>
                <td>{{ employee.nazwisko }}</td>
            </tr>
            <tr>
                <th>Imie</th>
                <td>{{ employee.imie }}</td>
            </tr>
            <tr>
                <th>Dataur</th>
                <td>{{ employee.dataUr }}</td>
            </tr>
            <tr>
                <th>Miejsceur</th>
                <td>{{ employee.miejsceUr }}</td>
            </tr>
            <tr>
                <th>Miejscezam</th>
                <td>{{ employee.miejsceZam }}</td>
            </tr>
            <tr>
                <th>Ulica</th>
                <td>{{ employee.ulica }}</td>
            </tr>
            <tr>
                <th>Obywatelstwo</th>
                <td>{{ employee.obywatelstwo }}</td>
            </tr>
            <tr>
                <th>Nrpaszportu</th>
                <td>{{ employee.nrPaszportu }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ employee.status }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('employee_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('employee_edit', { 'id': employee.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
