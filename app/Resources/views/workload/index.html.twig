{% extends 'base.html.twig' %}

{% block body %}
    <h1>Workloads list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Hoursinmnth</th>
                <th>Rate</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for workLoad in workLoads %}
            <tr>
                <td><a href="{{ path('workload_show', { 'id': workLoad.id }) }}">{{ workLoad.id }}</a></td>
                <td>{{ workLoad.hoursInMnth }}</td>
                <td>{{ workLoad.rate }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('workload_show', { 'id': workLoad.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('workload_edit', { 'id': workLoad.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('workload_new') }}">Create a new workLoad</a>
        </li>
    </ul>
{% endblock %}
